export type Sources = {
  mangaplus: {
    available: boolean;
  };
  mangasaki: {
    available: boolean;
  };
  sailmg: {
    available: boolean;
  };
};

export const sources: Sources = {
  mangaplus: {
    available: true,
  },
  mangasaki: {
    available: false,
  },
  sailmg: {
    available: true,
  },
};
